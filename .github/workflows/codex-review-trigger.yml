name: codex-review-trigger

on:
  pull_request_target:
    types: [opened, reopened, ready_for_review, synchronize]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  request-review:
    name: request-codex-review
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false

    steps:
      - name: Ensure @codex review comment exists
        uses: actions/github-script@v7
        with:
          script: |
            const marker = '<!-- codex-review-trigger -->';
            const mention = '@codex review';
            const issueNumber = context.payload.pull_request.number;
            const { owner, repo } = context.repo;

            const comments = await github.paginate(
              github.rest.issues.listComments,
              {
                owner,
                repo,
                issue_number: issueNumber,
                per_page: 100,
              }
            );

            const hasMention = comments.some((comment) =>
              typeof comment.body === 'string' &&
              comment.body.toLowerCase().includes(mention)
            );

            if (hasMention) {
              core.info('Found existing @codex review comment.');
              return;
            }

            const body = `${marker}\n${mention} for regressions in correctness, reliability, security, and cost.\n\nContext:\n- Follow CHARTER.md and AGENTS.md\n- Prioritize P0/P1 findings\n- Flag behavior regressions and missing tests`;

            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: issueNumber,
              body,
            });
